# Triage — 008-ai-assisted-initiative-management-adr-library

Source: `work/inputs/008-ai-assisted-initiative-management-adr-library.zip`
Applicable scaffolds: `tpl-decisions.md`, `tpl-doc-architecture.md`, `tpl-doc-spec.md`
Pass 1 completed: 02.04.2026
Status: AWAITING REVIEW

---

> **Source note:** The zip contains 9 ADR files (DEC-001 through DEC-009) plus
> a DECISIONS.md index. All ADR files use the old system's naming convention
> (`DEC-NNN-*.md`). This was the "frontend" assistant's decision log — its
> primary store was a local markdown repo (the "planning layer"), not the
> backend identity service that drives the current redesign. Where a decision
> is marked **superseded by new design intent** in the Notes column, the
> rationale behind the rejection may still be worth preserving as a
> `[REJECTED]` entry in inventory.

---

| # | Source (section / quote fragment) | Classification | Candidate tag | Notes | Cross-input notes |
|---|-----------------------------------|----------------|---------------|-------|-------------------|
| 1 | DEC-001: "Standalone repository. Initiatives are a distinct concern from any codebase or documentation they touch." | C | [DECISION] | Old design kept INI files in a local git repo. New design purposely avoids local storage; INI identity lives in the backend service. Useful as `[REJECTED]` rationale. | |
| 2 | DEC-001: "Cross-referencing between this repo and other repos is by link, not by directory proximity." | A | [CONSTRAINT] | Cross-system referencing by stable identifier, not path proximity — principle carries forward. Maps to Architecture.md Integration Points. | |
| 3 | DEC-002: "decisions/ (this folder): decisions about the repository itself — schema, tooling, naming, process conventions." | C | [DECISION] | Three-tier decision-log scope (repo / initiative / arch) was specific to the local-repo design. New design collapses the "repo" tier; all decisions land in `decisions/`. | |
| 4 | DEC-002: "Individual INI files: decisions about the scope, approach, and execution of that initiative." | B | [DECISION] | Initiative-level decision trail is still needed; in new design this moves to a Confluence page tree or structured field rather than a local INI file. Maps to Architecture.md (component responsibilities) and Spec.md (domain). | |
| 5 | DEC-002: "ADRs (in INI-003 / Confluence): architectural decisions about the application landscape and engineering standards." | A | [DECISION] | Confirms Confluence as the ADR home. Consistent with current design. Maps to decisions/NNN-*.md context section. | |
| 6 | DEC-003: "`AGENTS.md` at repo root is the mandatory first read for any AI agent session." | C | [MECHANISM] | Mechanism was git-repo-specific. Equivalent in new design is the agent's system prompt / CLAUDE.md chain. Superseded by architecture; retain as pattern reference. | |
| 7 | DEC-003: "Stale bootstrap information is worse than none — it will cause an agent to confidently apply outdated conventions." | B | [CONSTRAINT] | Principle (stale context is actively harmful) is architecture-grade. Maps to Architecture.md (agent context loading) and Spec.md (NFR: correctness / staleness). | |
| 8 | DEC-004: "Original toolchain split initiative management across three tools: GitHub Copilot (Intake/Execute), ChatGPT Enterprise browser (Review), and M365 Copilot (stakeholder comms)." | C | [REJECTED] | Describes a rejected multi-tool split from prior iteration. Background context only; does not map to current design documents. | |
| 9 | DEC-004: "Codex in VS Code … is the primary tool for all three modes — Intake, Review, and Execute." | C | [REJECTED] | Toolchain choice (Codex/VS Code) is superseded by the current design's Claude / Cowork approach. Useful as rejected-alternative rationale in decisions/ ADR for agent toolchain. | |
| 10 | DEC-004: "`generate-context.sh` is retained as a fallback for browser-based review sessions." | C | [REJECTED] | Context-bundle script was a workaround for limited file access. New design has direct MCP-level access; no equivalent needed. | |
| 11 | DEC-005: "Initiatives were tracked solely in markdown files in this repo, creating a silo." | C | [DECISION] | Root cause of the redesign: local-only storage creates maintenance burden and visibility gaps. Strong `[REJECTED]` candidate for the new architecture's storage decision. | |
| 12 | DEC-005: Four-layer model table — Planning (markdown repo) / Portfolio (ServiceNow) / Execution (Jira) / Documentation (Confluence) | B | [DECISION] | The three non-local layers (ServiceNow, Jira, Confluence) are directly relevant. The "Planning" layer shifts from local markdown to the backend service. Maps to Architecture.md (Component Overview) and Spec.md (domain decomposition). | |
| 13 | DEC-005: "ServiceNow: No API access currently. Integration is a manually maintained record, populated from a rendered template." | A | [CONSTRAINT] | No ServiceNow API access is a hard constraint carried forward. Maps to Architecture.md Integration Points (ServiceNow row) and Spec.md CON-xxx. | |
| 14 | DEC-005: "The template tool will be built with a stubbed `--push` flag so that when API access arrives … only the transport layer needs wiring." | A | [MECHANISM] | Design-for-future-API-access principle carries forward. New backend service should similarly stub ServiceNow push. Maps to Architecture.md (Integration Points / ServiceNow). | |
| 15 | DEC-005: "Jira: One shared organisational Space … Epics map to initiative phases; Stories/Tasks map to action items." | A | [DECISION] | Jira project structure confirmed. One Epic per initiative (refined in DEC-009), Stories per phase. Maps to Architecture.md (Jira integration). | |
| 16 | DEC-005: "Jira is authoritative for execution status once an initiative is Active." | A | [CONSTRAINT] | Jira as execution-status source of truth is a constraint on the backend service's sync logic. Maps to Architecture.md (data flow / status ownership). | |
| 17 | DEC-005: "Confluence: Personal space as workbench … pages promoted to permanent domain space when mature." | A | [DECISION] | Confirmed Confluence personal-space-first workflow. Relevant to the assistant's Confluence write operations. Maps to Architecture.md (Confluence integration). | |
| 18 | DEC-005: "Status ownership: ServiceNow holds portfolio-level status (Active, Blocked, Done). Jira holds task-level status." | B | [CONSTRAINT] | Status ownership split is a cross-system constraint. Relevant to both Architecture.md (Integration Points) and Spec.md (domain: status lifecycle). | |
| 19 | DEC-005: "INI schema gains three back-link fields: `ServiceNow Record`, `Jira Epics`, `Confluence Page`" | B | [MECHANISM] | Back-link fields in the INI record are carried forward into the backend service's data model. Maps to Architecture.md (Data Model / system/Data Model.md). | |
| 20 | DEC-006: "The designated project is IT-Architecture (team-managed; admin rights held). Isolation uses three layers: custom field `Initiative ID`, label `ini-managed`, back-references in INI file." | A | [CONSTRAINT] | Three-layer Jira isolation strategy is a hard constraint for the new backend's Jira API tooling. Maps to Architecture.md (Jira integration) and Spec.md (CON). | |
| 21 | DEC-006: "API tooling must be strictly scoped to issues it creates." | A | [CONSTRAINT] | Blast-radius constraint on Jira API operations. Maps to Architecture.md (Jira component: interfaces/constraints). | |
| 22 | DEC-006: "ServiceNow saved report … exported as CSV on demand. A new tool reads that export and produces a local Excel snapshot." | A | [MECHANISM] | ServiceNow ingest via CSV export is the current integration mechanism. Superseded intent in new design (backend service should own this), but the CSV-as-source pattern may persist until API access is available. Maps to Architecture.md (Integration Points / ServiceNow). | |
| 23 | DEC-007: "`<scope>/<nnn>-<slug>` branch naming; per-scope sequence numbers; individual decision files." | C | [DECISION] | Branching convention was git-repo-specific. New design uses same `decisions/NNN-*.md` file convention (already adopted). Branch naming is not relevant to the backend design. | |
| 24 | DEC-007: "One branch per cohesive change, named after the decision doc or work item it introduces." | C | [DECISION] | Working practice for the local git repo. Out of scope for the system architecture. | |
| 25 | DEC-008: "Five statuses — Draft, Scoping, Active, Blocked, Done" | A | [DECISION] | Initiative lifecycle statuses are directly applicable. Maps to Spec.md (domain: status lifecycle) and Architecture.md (data flow: status transitions). | |
| 26 | DEC-008: Gate criteria table — Draft→Scoping (4 conditions), Scoping→Active (5 conditions), Active→Done (3 conditions), Active→Blocked (3 conditions), Blocked→Active (2 conditions) | A | [DECISION] | Full status transition gate logic. Directly feeds Spec.md (functional requirements) and Architecture.md (data flow). | |
| 27 | DEC-008: "Blocked is reserved for Active initiatives. Draft and Scoping initiatives with unresolved gaps remain in their current status." | A | [CONSTRAINT] | Semantic constraint on Blocked state. Carries directly into the backend service's state machine. Maps to Architecture.md (data flow) and Spec.md (domain). | |
| 28 | DEC-008: "Jira Epics created — or Epic creation is the documented first action immediately following the transition; the INI file's `Jira Epics` field must be populated before the initiative is considered fully Active." | A | [CONSTRAINT] | Jira Epic linkage is a hard gate for Active status. Backend service must enforce or at minimum validate this. Maps to Architecture.md (data flow / status transitions). | |
| 29 | DEC-008: "ServiceNow record: update if one exists; creation is not required to go Active." | A | [CONSTRAINT] | ServiceNow is not a gate for Active status. Reduces sync complexity requirement. Maps to Architecture.md (Integration Points / ServiceNow). | |
| 30 | DEC-008: "ServiceNow is deliberately excluded as a gate because it is maintained manually for visibility and budgeting, not as an execution signal." | A | [CONSTRAINT] | Rationale for ServiceNow non-gate status. Confirms ServiceNow is read/visibility-only in the current design. Maps to Architecture.md (Integration Points). | |
| 31 | DEC-009: "Add `## Phases` section to the INI schema; rename `## Notes / Next Actions` → `## Staging`." | B | [DECISION] | INI schema fields (Phases, Staging) map to the backend service's initiative data model. Maps to system/Data Model.md and Architecture.md (component: initiative record). | |
| 32 | DEC-009: Jira work item hierarchy — Initiative→Epic, Phase→Story (child of Epic), Staging item→Sub-task | A | [DECISION] | Jira hierarchy is a confirmed design choice. Maps to Architecture.md (Jira integration) and system/Data Model.md (Jira entity mapping). | |
| 33 | DEC-009: "ITARCH is a team-managed (next-gen) Jira project. In team-managed projects, Tasks cannot be children of Stories — only Sub-tasks can." | A | [CONSTRAINT] | Jira project type constraint on work item hierarchy. Hard platform constraint. Maps to Architecture.md (Jira integration). | |
| 34 | DEC-009: "One Epic per INI. Phase grouping is handled by Stories." | A | [DECISION] | Supersedes DEC-005's implied multi-Epic model. One Epic = one initiative in Jira. Maps to Architecture.md and system/Data Model.md. | |
| 35 | DEC-009: "Phase 0 — Staging is not listed in `## Phases`. It is created automatically by `publish-initiative` when `## Staging` contains unpublished items." | A | [MECHANISM] | Tool-owned implicit phase pattern. In new design, the backend service takes this role. Maps to Architecture.md (agent/tool responsibilities). | |
| 36 | DEC-009: "After `publish-initiative` runs, published `[ ]` lines are removed and replaced with a JQL back-reference comment." | A | [MECHANISM] | Staging-section cleanup-on-publish pattern. Equivalent behaviour should be in the backend service's Jira publish flow. Maps to Architecture.md (data flow). | |
| 37 | DEC-009: "`## Staging` serves items pending Jira creation only — ephemeral by design; items leave the INI once published." | A | [CONSTRAINT] | Staging section is explicitly ephemeral. INI record in new design should enforce same separation: planning content persists, execution items are pushed and cleared. Maps to Architecture.md and Spec.md. | |
| 38 | DEC-009: Alternatives considered — "Keep Notes/Next Actions … mixing them makes the tool's parsing fragile." | A | [REJECTED] | Confirmed rejected pattern: mixed free-form + structured content in one section. Relevant constraint for the new design's initiative schema. Maps to system/Data Model.md (schema design rationale). | |
| 39 | DEC-009: "`customfield_10422` = INI-ID (Initiative ID custom field in Jira)" | A | [CONSTRAINT] | Specific Jira custom field identifier for INI cross-referencing. Hard operational constraint for the Jira integration component. Maps to Architecture.md (Jira integration). | |
| 40 | DECISIONS.md index — nine decisions spanning 23.03.2026–28.03.2026 | C | | Index file only; no independent claims. Provenance / chronology reference. | |
| 41 | DEC-005 / DEC-006 / DEC-009: No mention of a backend identity service; INI-NNN identifiers are assigned by the local repo (file naming convention) | D | [OPEN] | **Gap vs. new design**: The old system has no concept of a centralised identity/mapping service. The backend service's identity assignment mechanism, API surface, and cross-system mapping store are absent from all source ADRs. This is the most significant structural gap between old and new design. | |
| 42 | DEC-005 / DEC-008 / DEC-009: All state transitions and tool operations assume direct filesystem access to INI markdown files | D | [OPEN] | **Gap vs. new design**: No ADR addresses how an agent reads or writes initiative state when there is no local file. The new design's read-path (agent queries backend instead of reading files) has no precedent in this library. | |
| 43 | DEC-006: ServiceNow CSV ingest produces a local Excel snapshot — no ADR addresses conflict resolution or staleness between snapshot and live ServiceNow | D | [OPEN] | Gap: snapshot consistency / staleness policy not addressed. New backend service will need an explicit decision here. | |
| 44 | DEC-005: "Confluence: Personal space as workbench … pages promoted to permanent domain space when mature." No ADR defines the promotion trigger, criteria, or agent role in promotion. | D | [OPEN] | Gap: Confluence promotion lifecycle not specified. Relevant to Architecture.md (Confluence integration). | |
| 45 | tpl-doc-architecture.md requires Guiding Principles section — not present in any source ADR | D | | Gap vs. architecture scaffold: no explicit architectural guiding principles stated. DEC-003 #7 (stale context is harmful) is the closest — implies a freshness principle. | |
| 46 | tpl-decisions.md requires Decider(s) and Document context fields — absent from all source ADRs | D | | Gap vs. ADR scaffold: source ADRs have no Decider field and no link to a triggering document. When migrating lessons learned to new decisions/, these fields must be added. | |
